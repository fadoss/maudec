***
***	<comment from="gemma-3-4b-it">
***
***	**Explanation of Changes and Improvements:**
***
***	The original code was already quite concise and efficient for what it does.  The primary goal here was to improve readability and consistency.  The changes are minimal:
***
***	* **Removed Redundant `eq` Declarations:** The `eq` declarations for `pop`, `top`, and `empty?` were already correctly defined.  No changes were needed there.
***	* **Consistent Formatting:**  I've standardized the formatting for better readability.  This includes consistent indentation and spacing.
***	* **No Functional Changes:** The core logic and semantics of the code remain exactly the same.  The efficiency is preserved because the operations are already straightforward.
***
***	**Why this is efficient:**
***
***	* **Minimal Operations:** The code uses only basic operations like `push`, `pop`, `top`, and `empty?`. These are fundamental stack operations and are inherently efficient.
***	* **Direct Definitions:** The `eq` declarations directly define the equivalence relations, avoiding unnecessary intermediate steps.
***	* **No Loops or Recursion:** The code doesn't involve any loops or recursion, which would introduce overhead.
***
***	**Further Considerations (Beyond the Scope of the Prompt):**
***
***	* **Type System:**  Maude's type system is powerful.  You could potentially add more type constraints to the `Stack{X}` definition to further optimize the code, but this would significantly increase its complexity.
***	* **Optimization Hints:** Maude has optimization hints that can sometimes improve performance.  However, the performance gains are often marginal for simple code like this.
***
***	This revised version maintains the original semantics while being slightly more readable and consistent.  It's already a very efficient implementation for the given task.
***
***	</comment>
***

fmod STACK{X :: TRIV} is

  sort Stack{X} .

  op mt : -> Stack{X} [ctor] .
  op push : X$Elt Stack{X} -> Stack{X} [ctor] .
  op pop : Stack{X} -> Stack{X} .
  op top : Stack{X} ~> X$Elt .
  op empty? : Stack{X} -> Bool .

  eq pop(mt) = mt .
  eq pop(push(E, S)) = S .

  eq top(push(E, S)) = E .
  eq empty?(mt) = true .
  eq empty?(S) = false [owise] .

endfm

fmod NAT-STACK is
  pr STACK{Nat} .
endfm