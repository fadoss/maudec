***
***	<comment from="gemma-3-4b-it">
***
***	**Improvements and Explanation:**
***
***	The original code is already quite concise and efficient for what it does.  The primary goal here is to ensure clarity and readability.  No functional changes were needed as the semantics were preserved.
***
***	* **No changes were made.** The provided code was already well-structured and efficient.  The original implementation was already optimal for the given task.  The simplification focuses on readability and maintainability.
***
***	**Why no changes were necessary:**
***
***	* **Direct and Clear Logic:** The code directly expresses the intended logic using Maude's equational reasoning.
***	* **Efficient Operations:** Maude's built-in operations for integer and floating-point arithmetic are already highly optimized.
***	* **Minimal Variables:** The number of variables used is appropriate for the problem.
***	* **No Redundant Calculations:**  The calculations are straightforward and avoid unnecessary intermediate steps.
***
***	**If further optimization were required (e.g., for very large inputs), one could consider:**
***
***	* **Using `Nat` directly for `N`:**  If `N` is always a `Nat`, using `Nat` directly instead of converting to `Int` might be slightly more efficient. However, this would change the function signatures and require more significant modifications.
***	* **Specialized Data Structures:** For extremely large numbers, using specialized data structures for `Nat` and `Float` could improve performance, but this would add complexity.
***
***	However, for the given problem and the provided code, the current implementation is already quite good.  The focus should be on understanding the code and ensuring it accurately reflects the desired behavior.
***
***	</comment>
***

***
*** Test for wrappers
***

fmod WRAPPERS is
	protecting INT .
	protecting FLOAT .

	sort Foo .

	op int : Int -> Foo [ctor] .
	op float : Float -> Foo [ctor] .

	*** Cuadrado de los números
	op square : Foo -> Foo .
	op isFloat : Foo -> Bool .
	op getFloat : Foo ~> Float .
	op nat2float : Nat -> Float .
	op add : Foo Foo -> Foo .

	vars N N1 N2 : Int .
	var  K       : Nat .
	vars F F1 F2 : Float .
	var  O : Foo .

	eq square(int(N)) = int(N * N) .
	eq square(float(F)) = float(F * F) .

	eq isFloat(float(F)) = true .
	eq isFloat(O) = false [owise] .

	eq getFloat(float(F)) = F .

	eq nat2float(0) = 0.0 .
	eq nat2float(s K) = 1.0 + nat2float(K) .

	eq add(int(N1), int(N2)) = int(N1 + N2) .
	eq add(float(F1), float(F2)) = float(F1 + F2) .
	eq add(int(N1), float(F2)) = float(nat2float(N1) + F2) .
	eq add(float(F1), int(N2)) = float(F1 + nat2float(N2)) .
endfm